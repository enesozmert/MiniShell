#include "../../include/header.h"

int main(int argc, char const *argv[])
{
	(void)argc;
	(void)argv;

	// pid_t pid;

	// pid = fork();
	// if (pid < 0)
	// 	exit(-1);
	// else if (pid > 0)
		routine();
	return 0;
}
