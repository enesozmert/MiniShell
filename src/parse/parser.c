#include "../../include/header.h"

void parser(t_rdl *rdl)
{
	// int i;
	// int j;
	// int len = ft_strlen(rdl->main_str);

	// i = -1;
	// j = -1;
	// while (++i < len)
	// {
	// 	if (is_operator(rdl, strdup(rdl->main_str)))
	// }
	// print_token(rdl->token);
}

t_rdl *parser_analizer(t_rdl *rdl)
{
	(void)rdl;
	return (NULL);
}
